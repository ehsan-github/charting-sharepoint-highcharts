export const constant = {};

export const setTitle = text => {
    return {
        title: {
            text
        }
    };
};

export const setSubTitle = text => {
    return {
        subtitle: {
            text
        }
    };
};
